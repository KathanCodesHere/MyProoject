import React from "react";

const about = () => {
  return (
    <div>
      <h2>Cloning a repository</h2>
      <p>
        When you create a repository on GitHub, it exists as a remote
        repository. You can clone your repository to create a local copy on your
        computer and sync between the two locations.
      </p>
    </div>
  );
};

export default about;
